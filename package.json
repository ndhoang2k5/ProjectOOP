{
  "name": "projectoop",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm:start-frontend\" \"npm:start-backend\"",
    "start-frontend": "cd frontend && npm install && npm start",
    "start-backend": "cd backend && mvn install && mvn compile exec:java"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
